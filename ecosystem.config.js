module.exports = {
  apps: [
    {
      name: 'whatsapp-api', // اسم التطبيق كما سيظهر في قائمة PM2
      script: './dist/server.bundle.js', // مسار الملف النهائي المدمج
      
      // 🌟 [حماية ضد التعدد]: هذا الإعداد يضمن تشغيل نسخة واحدة فقط
      instances: 1, 
      exec_mode: 'fork', // استخدام وضع fork بدلاً من cluster لمنع أي تضارب
      
      // إعدادات البيئة
      env: {
        NODE_ENV: 'production',
        PORT: 3000
      },

      // إعدادات إعادة التشغيل التلقائي (Auto Restart)
      autorestart: true, // إعادة التشغيل فوراً إذا توقف السيرفر لسبب مفاجئ
      watch: false, // تعطيل المراقبة (لا نريد إعادة التشغيل عند كل تعديل بسيط في الملفات في الـ Production)
      max_restarts: 10, // الحد الأقصى لمحاولات إعادة التشغيل السريعة قبل التوقف لتجنب الحلقات المفرغة
      
      // إدارة الذاكرة (اختياري ولكنه مفيد جداً لمشاريع الواتساب)
      max_memory_restart: '1G', // إعادة تشغيل السيرفر بأمان إذا تجاوز استهلاك الذاكرة 1 جيجابايت لتفريغ الكاش
      
      // إعدادات السجلات (Logs)
      out_file: './logs/out.log', // مسار ملف سجلات التشغيل الطبيعية
      error_file: './logs/error.log', // مسار ملف سجلات الأخطاء
      time: true // إضافة طابع زمني (Timestamp) لكل سطر في السجلات
    }
  ]
};